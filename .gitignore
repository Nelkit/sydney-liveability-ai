# Environment variables
.env
.env.local
.env.*.local

# API Keys and Secrets
*.key
*.pem

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
venv/
venv-notebooks/
ENV/
env/
.venv

# IDE
.idea/
*.swp
*.swo
*~
.DS_Store

# Node.js
node_modules/
npm-debug.log
yarn-error.log
.next/
out/
.nuxt
dist/
# TypeScript incremental build cache
*.tsbuildinfo

# Data (local storage - ignored in Git)
data/raw/abs/
!data/processed/suburbs.geojson

# Logs
logs/
*.log
d
# OS
.DS_Store
Thumbs.db

# Arctic Shift dump files (large, downloaded locally)
data/raw/arctic_shift/

# Community report raw PDFs (large, local only — per AGENTS.md)
data/raw/community_reports/*.pdf

# Pre-processed Reddit data and NLP analysis results are committed
# as carve-out exceptions to the "no data/processed/" rule so teammates
# and graders can run the chat demo without re-running extraction or
# downloading the ~1.8 GB of transformer models the pipeline needs.
# See AGENTS.md for the full list of exceptions.

# ChromaDB local vector store (machine-local, binary — regenerate with ingest scripts)
backend/data/chromadb/

/openspec
.claude/*
!.claude/skills/
.codex